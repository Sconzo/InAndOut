@model IEnumerable<InAndOut.Models.ExpenseType>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Expenses Control</h2>

        </div>
        <div class="col-6" text-right>
            <a class="btn btn-primary" asp-controller="ExpenseType" asp-action="Create">Add new Expense Type</a>
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Expense Type
                    </th>
                    <th>
                        Changes
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="35%"> @obj.ExpenseTypeName</td>
                        <td width="20%">
                            <div class="w-100 btn-group" role="group">
                                <a class="btn btn-primary mx-1" asp-route-Id="@obj.Id" asp-controller="ExpenseType" asp-action="Update">Update</a>
                                <a class="btn btn-danger mx-1" asp-route-Id="@obj.Id" asp-controller="ExpenseType" asp-action="Delete">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No expenses Types yet</p>
    }

</div>

